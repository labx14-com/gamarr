# Keep Staticcheck focused on correctness for this codebase. Error text is part of
# user-visible API/job detail in several paths, so sentence capitalization is intentional.
# Numeric HTTP assertions are also kept readable in tests.
checks = ["inherit", "-ST1005", "-ST1013"]
