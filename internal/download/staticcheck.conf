# The two SA2001 findings in manager_test.go deliberately use the mutex as a
# synchronization barrier while waiting for asynchronous Minerva cleanup.
# Keep that test idiom local to this package rather than disabling SA2001 globally.
checks = ["inherit", "-SA2001"]
